NODE_ENV=development
PORT=4173

# Before production, change NODE_ENV to production and add both secrets:
# ADMIN_PASSWORD=replace-with-a-long-unique-password
# SESSION_SECRET=replace-with-a-long-random-secret
